<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.strojek.android.nextbiker"
    android:versionCode="1"
    android:versionName="0.1" >

    <uses-sdk android:minSdkVersion="8" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" >
    </uses-permission>
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" >
    </uses-permission>

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/NextBikerTheme" >
        <activity
            android:name="info.strojek.android.nextbiker.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="info.strojek.android.nextbiker.LookupActivity"
            android:alwaysRetainTaskState="true"
            android:label="@string/app_name" />
        <activity
            android:name="info.strojek.android.nextbiker.RentialActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".nextbiker.WebLoginActivity"
            android:label="@string/app_name" />
    </application>

</manifest>